import styled from "styled-components";

export const Wrapper = styled.section`
  background: var(--light-alpha);
  border-radius: 1rem;
  width: 22.3rem;
  min-height: 17.8rem;
  padding: 2.4rem;
  display: flex;
  flex-wrap: wrap;
  gap: 0.8rem;
`;
