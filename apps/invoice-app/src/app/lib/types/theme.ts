export type ThemeOptions = "light" | "dark";

export interface ThemeProps {
  background: string;
  text: string;
  headers: string;
  date: string;
  invoiceItem: string;
  draftText: string;
  draftBg: string;
  invoiceTable: string;
  invoiceTableTotal: string;
}
